import { WsDef } from '../ws-def';
import { DynamicField } from '../dynamic-form/dynamic-field';

export class HtmlModalDef {
    // Obligatorios
    attributeMapping: string; // Se mapea el atributo de la entidad que contiene el html con este mapeo
}
